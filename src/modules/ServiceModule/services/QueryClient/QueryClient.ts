import {
  FetchQueryOptions,
  QueryClient,
  QueryFunction,
  createQueryClient,
} from '@example/shared';

export const queryClient = createQueryClient();

export { QueryClient, QueryFunction, FetchQueryOptions };
